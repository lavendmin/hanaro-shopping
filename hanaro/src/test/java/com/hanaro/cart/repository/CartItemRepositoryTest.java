package com.hanaro.cart.repository;

import com.hanaro.RepositoryTest;

class CartItemRepositoryTest extends RepositoryTest {
	CartItemRepository cartItemRepository;

	/*
	@Test
	void addTest() {
		long preCount = cartItemRepository.count();

		Item item = Item.builder()
			.name("itemTest")
			.price(1000)
			.stock(20)
			.description("itemTest-des")
			.discount(10.0).build();

		Cart cart = Cart.builder()
				.customer()

		Stream.iterate(1, n -> n + 1).limit(5)
			.map(n -> CartItem.builder()
				.item(item)
				.cart())
	}
	 */
}
